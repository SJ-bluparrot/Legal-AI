# FastAPI and server
fastapi>=0.104.0
uvicorn>=0.24.0

# Pydantic for data validation
pydantic>=2.5.0

# Anthropic Claude API — all AI inference (Haiku for chat/extraction, Sonnet for complaints)
# Install: pip install anthropic
# Set key: export ANTHROPIC_API_KEY=sk-ant-...
anthropic>=0.40.0

# Environment variable management — loads .env file at startup
python-dotenv>=1.0.0

# Rate limiting — protects /questions from being hammered
slowapi>=0.1.9

# Streamlit frontend
streamlit>=1.32.0

# HTTP client — used by backend_client.py to call the FastAPI backend
requests>=2.31.0

# PDF generation
reportlab>=4.0.0

# MongoDB Atlas — replaces SQLite for all persistent storage
pymongo[srv]>=4.6.0

# Testing
pytest>=7.4.0
